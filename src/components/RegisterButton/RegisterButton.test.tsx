import React from 'react';
import ReactDOM from 'react-dom';
import RegisterButton from './RegisterButton';

// it('It should mount', () => {
//   const div = document.createElement('div');
//   ReactDOM.render(<RegisterButton  />, div);
//   ReactDOM.unmountComponentAtNode(div);
// });